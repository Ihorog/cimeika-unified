---
source: Ihorog/cimeika
archived: 2026-02-04
reason: System consolidation
---

api:
  base_url: "https://api.cimeika.com/v1"
  keys:
    openai: "${OPENAI_API_KEY}"
    huggingface: "${HUGGINGFACE_TOKEN}"
    telegram: "${TELEGRAM_BOT_KEY}"

modes:
  environment: "staging"  # або 'prod'
  debug: true
