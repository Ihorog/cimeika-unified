# Розгортання CIMEIKA на Vercel

Цей документ описує процес розгортання фронтенду CIMEIKA на платформі Vercel.

## 📋 Передумови

- Акаунт на [Vercel](https://vercel.com)
- GitHub репозиторій підключений до Vercel
- Backend розгорнуто окремо (наприклад, на Railway, Render або іншій платформі)

## 🚀 Швидке розгортання

### Крок 1: Підключення репозиторію

1. Увійдіть у свій акаунт Vercel
2. Натисніть "Add New Project"
3. Імпортуйте репозиторій `Ihorog/cimeika-unified`
4. Vercel автоматично визначить конфігурацію з `vercel.json`

### Крок 2: Налаштування змінних середовища

У налаштуваннях проєкту Vercel додайте наступні змінні середовища:

#### Обов'язкові змінні:

```
VITE_API_URL=https://your-backend-url.com
VITE_APP_NAME=CIMEIKA
```

#### Як додати змінні:

1. Перейдіть до Settings → Environment Variables
2. Додайте кожну змінну для всіх середовищ (Production, Preview, Development)
3. Збережіть зміни

### Крок 3: Розгортання

Після налаштування змінних середовища:

1. Натисніть "Deploy"
2. Vercel автоматично збудує та розгорне ваш проєкт
3. Після завершення ви отримаєте URL для доступу до додатку

## ⚙️ Конфігурація

Проєкт використовує файл `vercel.json` у корені репозиторію з наступною конфігурацією:

- **Framework**: Vite
- **Build Directory**: `frontend/dist`
- **Install Command**: `cd frontend && npm install`
- **Build Command**: `cd frontend && npm install && npm run build`
- **Dev Command**: `cd frontend && npm run dev`

## 🔄 Автоматичне розгортання

Vercel автоматично розгортає:

- **Production**: При push у гілку `main`
- **Preview**: При створенні Pull Request

## 🌍 Змінні середовища

### VITE_API_URL

URL вашого backend API. Має бути повний URL включно з протоколом.

**Приклади:**
- `https://cimeika-backend.railway.app`
- `https://api.cimeika.com`

⚠️ **Важливо**: Без цієї змінної фронтенд не зможе підключитися до backend.

### VITE_APP_NAME

Назва додатку, що відображається в інтерфейсі.

**За замовчуванням**: `CIMEIKA`

**Примітка**: Ця змінна зарезервована для майбутнього використання в UI (заголовки, titles тощо).

## 📁 Структура проєкту для Vercel

Vercel розгортає лише фронтенд частину проєкту:

```
cimeika-unified/
├── frontend/              # Розгортається на Vercel
│   ├── src/              # React додаток
│   ├── public/           # Статичні файли
│   ├── package.json      # Залежності
│   └── vite.config.js    # Конфігурація Vite
├── vercel.json           # Конфігурація Vercel
└── .vercelignore         # Виключення файлів
```

## 🔧 Локальна розробка з Vercel CLI

Встановіть Vercel CLI:

```bash
npm install -g vercel
```

Запустіть локально:

```bash
vercel dev
```

Це запустить локальний сервер розробки з налаштуваннями, що відповідають production.

## 📊 Моніторинг та логи

Перегляньте логи та метрики розгортання:

1. Відкрийте свій проєкт у Vercel Dashboard
2. Перейдіть до вкладки "Deployments"
3. Виберіть потрібне розгортання для перегляду деталей

## 🐛 Troubleshooting

### Помилка: "Build failed"

**Рішення:**
1. Перевірте логи збірки у Vercel Dashboard
2. Переконайтеся, що всі залежності у `package.json` коректні
3. Локально запустіть `npm run build` у папці `frontend`

### Помилка: "API connection failed"

**Рішення:**
1. Перевірте, що `VITE_API_URL` встановлено правильно
2. Переконайтеся, що backend доступний за вказаним URL
3. Перевірте CORS налаштування на backend

### Помилка: "Environment variables not working"

**Рішення:**
1. Переконайтеся, що змінні починаються з `VITE_`
2. Після зміни змінних потрібно перезібрати проєкт
3. Змінні доступні лише на етапі збірки

## 🔗 Корисні посилання

- [Vercel Documentation](https://vercel.com/docs)
- [Vite Documentation](https://vitejs.dev/)
- [CIMEIKA Repository](https://github.com/Ihorog/cimeika-unified)

## 📝 Додаткові налаштування

### Custom Domain

1. Перейдіть до Settings → Domains
2. Додайте ваш домен
3. Налаштуйте DNS записи відповідно до інструкцій Vercel

### Performance Optimization

Vercel автоматично оптимізує:
- Edge caching
- Compression (Gzip/Brotli)
- Image optimization
- Automatic HTTPS

## 🔐 Безпека

- Всі змінні середовища зашифровані
- HTTPS увімкнено автоматично
- Додайте API ключі лише через Vercel Environment Variables
- Ніколи не комітьте `.env` файли до репозиторію

## 📮 Підтримка

Якщо виникають проблеми:
1. Перевірте [Vercel Status](https://www.vercel-status.com/)
2. Перегляньте логи розгортання
3. Зверніться до документації проєкту

---

**Останнє оновлення:** Грудень 2024

**Версія документу:** 1.0.0
