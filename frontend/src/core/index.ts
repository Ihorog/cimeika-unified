/**
 * Core orchestration and interfaces
 * Central coordination layer for all Cimeika modules
 */

export { ModuleRegistry, registry } from './orchestrator';
export type { ModuleInterface, ModuleStatus, ModuleMetadata } from '../types';
