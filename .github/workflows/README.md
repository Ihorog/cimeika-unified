# Автоматичне розгортання на Vercel

Цей файл містить інструкції для налаштування автоматичного розгортання фронтенду на Vercel через GitHub Actions.

## 🎯 Що це дає?

✅ Автоматичний deployment при кожному push у `main`  
✅ Preview deployment для кожного Pull Request  
✅ Автоматичні коментарі з посиланнями на preview  
✅ Повна інтеграція з GitHub  
✅ Моніторинг через GitHub Actions

## 🚀 Швидке налаштування (5 хвилин)

### 1. Отримайте Vercel Token

```bash
# Перейдіть на https://vercel.com/account/tokens
# Створіть новий токен з назвою: github-actions
# Скопіюйте згенерований токен
```

### 2. Отримайте Project та Org ID

```bash
# Встановіть Vercel CLI
npm install -g vercel@latest

# Підключіть проєкт
cd frontend
vercel link

# Отримайте ID
cat .vercel/project.json
```

### 3. Додайте GitHub Secrets

Перейдіть до: **Settings** → **Secrets and variables** → **Actions**

Додайте три секрети:
- `VERCEL_TOKEN` - токен з кроку 1
- `VERCEL_ORG_ID` - `orgId` з кроку 2
- `VERCEL_PROJECT_ID` - `projectId` з кроку 2

### 4. Готово! 🎉

Тепер кожен push у `main` або PR автоматично запускає deployment.

## 📖 Детальна документація

➡️ [Повна інструкція з troubleshooting](../docs/GITHUB_ACTIONS_VERCEL.md)

## 🔍 Як перевірити що працює?

1. Зробіть зміну у `frontend/`
2. Створіть Pull Request
3. Перейдіть до вкладки **Actions**
4. Подивіться на виконання workflow
5. У PR з'явиться коментар з посиланням на preview

## ⚠️ Важливо

Не забудьте налаштувати змінні середовища у Vercel Dashboard:
- `VITE_API_URL` - URL вашого backend
- `VITE_APP_NAME` - Назва додатку

## 📞 Допомога

Якщо щось не працює:
1. Перевірте що всі 3 секрети додано правильно
2. Перегляньте логи у GitHub Actions
3. Прочитайте повну документацію вище

---

**Workflow файл:** `.github/workflows/vercel-deploy.yml`  
**Статус:** ✅ Активний
