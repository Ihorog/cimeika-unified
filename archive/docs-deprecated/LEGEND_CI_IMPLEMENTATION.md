# Легенда ci — Інтерактивна Веб-Реалізація

## 📋 Огляд

**Легенда ci** — це повноцінна інтерактивна веб-система для дослідження еволюції знань і сенсів через призму дуальності світобудови. Система реалізована як окремий модуль у складі Cimeika Unified і включає:

- 20 взаємопов'язаних вузлів з коротким і глибинним текстом
- Повний текст легенди "Тиша і перша іскра" (10 розділів)
- Інтерактивну радіальну навігацію
- Систему станів користувача (огляд, занурення, інтеграція)
- Персоналізацію та відстеження прогресу

---

## 🎯 Маршрут доступу

**URL**: `/ci/legend`

Доступ також через головну сторінку модуля Ci з prominent банером.

---

## 🏗️ Архітектура

### Backend (Python/FastAPI)

```
backend/app/modules/ci/
├── legend_ci_content.py      # 20 вузлів з контентом
├── legend_duality_full.py    # Повний текст легенди про дуальність
└── api.py                    # API endpoints для Legend ci
```

### Frontend (React)

```
frontend/src/modules/LegendCi/
├── views/
│   └── LegendCiView.jsx      # Головний view
├── components/
│   ├── RadialMap.jsx         # Радіальна навігаційна карта
│   └── NodeView.jsx          # Модальний перегляд вузла
├── services/
│   └── api.js                # API клієнт
└── store.js                  # Zustand store
```

---

## 🔌 API Endpoints

### Метадані легенди
```
GET /api/ci/legend
```
Повертає метадані: назву, структуру, принципи взаємодії.

### Список вузлів
```
GET /api/ci/legend/nodes
GET /api/ci/legend/nodes?tags=duality,philosophy
```
Повертає всі вузли або фільтрує за тегами.

### Окремий вузол
```
GET /api/ci/legend/nodes/{node_id}
```
Повертає повні дані вузла з інформацією про зв'язки.

### Легенда про дуальність
```
GET /api/ci/legend/duality
GET /api/ci/legend/duality/sections/{section_id}
```
Повертає повний текст або окрему секцію.

### Символічна бібліотека
```
GET /api/ci/legend/symbols
```
Повертає значення чисел, геометричних форм та елементів.

### Навігація
```
GET /api/ci/legend/navigation/{current_node_id}?state=overview
```
Повертає доступні шляхи навігації залежно від стану користувача.

---

## 📊 Структура даних

### Вузол легенди

```javascript
{
  "id": 1,
  "title": "Тиша і Можливість",
  "short_text": "У тиші немає порожнечі...",
  "deep_text": "Колись не було нічого...",
  "icon": "🌑",
  "tags": ["origin", "potential", "quantum"],
  "visual_hint": "темне поле з ледь помітним шумом",
  "motion_scenario": "поява точки з темряви",
  "connections": [2, 7, 8]
}
```

### Секція легенди про дуальність

```javascript
{
  "id": "section_1",
  "title": "🌑 Тиша і перша іскра 🌒",
  "content": "Повний текст секції...",
  "icon": "🌑🌒"
}
```

---

## 🎨 Візуальний дизайн

### Кольорова палітра

- **Primary Gold**: `#f59e0b`
- **Dark Gold**: `#d97706`
- **Light Gold**: `#fbbf24`
- **Background Light**: `#fef3c7`
- **Background Extra Light**: `#fffbeb`
- **Text Primary**: `#92400e`
- **Text Secondary**: `#78350f`

### Компоненти

#### RadialMap
- Інтерактивна canvas-based радіальна карта
- Центр: "Ci" (30px радіус)
- Вузли розташовані по колу (25px радіус кожен)
- З'єднання між вузлами (напівпрозорі лінії)
- Hover ефекти з tooltip
- Клік → відкриття NodeView

#### NodeView
- Модальне вікно на повний екран
- Градієнтний header з іконкою та назвою
- Перемикач шарів: короткий ↔ глибинний
- Теги з візуальними підказками
- Сітка пов'язаних вузлів для навігації

#### StateSelector
- Три кнопки станів: Огляд / Занурення / Інтеграція
- Активний стан підсвічено градієнтом
- Впливає на рекомендації навігації

#### ProgressBar
- Відображає відсоток досліджених вузлів
- Градієнтна заливка
- Текстова інформація: "Досліджено: X з 20 вузлів"

---

## 🔄 Стани користувача

### 1. Огляд (Overview)
**Мета**: Побачити всю структуру легенди.

**Рекомендації**: Початкові вузли (1, 2, 8).

**Поведінка**:
- Радіальна карта повністю видима
- Всі вузли доступні для кліку
- Структурний огляд за розділами

### 2. Занурення (Immersion)
**Мета**: Глибоке дослідження окремого вузла.

**Рекомендації**: Вузли з схожими тегами до поточного.

**Поведінка**:
- Автоматично переходить при першому кліку на вузол
- NodeView з глибинним текстом
- Зв'язки з іншими вузлами

### 3. Інтеграція (Integration)
**Мета**: Синтез і зв'язування всіх вузлів.

**Рекомендації**: Фінальні вузли (17, 19, 20).

**Поведінка**:
- Показує всю траєкторію дослідження
- Підсумкова картина всієї легенди
- Усвідомлення зв'язків між усіма вузлами

---

## 💾 Персоналізація

### Zustand Store
Зберігає локально:
- `visitedNodes`: масив ID відвіданих вузлів
- `currentNode`: поточний активний вузол
- `userState`: поточний стан користувача
- `layerDepth`: короткий або глибинний текст
- `visualMode`: радіальний або лінійний режим

### Прогрес
Формула: `(visitedNodes.length / totalNodes.length) * 100`

Візуалізація: progress bar + текстовий індикатор.

---

## 🎬 Анімації та переходи

### fadeIn (overlay)
```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

### slideUp (modal)
```css
@keyframes slideUp {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}
```

### float (hero icon)
```css
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}
```

### pulse (loading)
```css
@keyframes pulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.5; transform: scale(1.1); }
}
```

---

## 📱 Responsive дизайн

### Breakpoints
- **Desktop**: >768px (повна функціональність)
- **Mobile**: ≤768px (адаптивні компоненти)

### Адаптації
- Радіальна карта масштабується до ширини екрану
- Кнопки станів стають вертикальними
- Модальні вікна займають 100% ширини з відступами
- Структурна сітка переходить в одну колонку

---

## 🚀 Використання

### Для користувачів

1. **Вхід**: Відкрити `/ci` → клікнути на банер "✨ Відкрити Легенду"
2. **Огляд**: Роздивитися радіальну карту всіх 20 вузлів
3. **Вибір стану**: Обрати "Огляд" / "Занурення" / "Інтеграція"
4. **Дослідження**: Клікнути на вузол для відкриття
5. **Навігація**: Використовувати зв'язки для переходу між вузлами
6. **Шари**: Перемикати короткий ↔ глибинний текст

### Для розробників

#### Імпорт компонентів
```javascript
import { LegendCiView, RadialMap, NodeView } from '@/modules/LegendCi';
```

#### Використання store
```javascript
import useLegendCiStore from '@/modules/LegendCi/store';

const { 
  currentNode, 
  visitedNodes, 
  getProgress 
} = useLegendCiStore();
```

#### API виклики
```javascript
import { legendCiApi } from '@/modules/LegendCi/services/api';

const nodes = await legendCiApi.getNodes();
const node = await legendCiApi.getNode(1);
const duality = await legendCiApi.getDualityLegend();
```

---

## 📈 Майбутні покращення

### Фаза 2
- [ ] Збереження траєкторії користувача на backend
- [ ] Аналітика: популярні шляхи дослідження
- [ ] Експорт персональної карти легенди (PDF/PNG)
- [ ] Голосове читання легенди

### Фаза 3
- [ ] Мультимовна підтримка (EN, UA)
- [ ] 3D-візуалізація замість 2D canvas
- [ ] VR/AR режим для повного занурення
- [ ] Соціальні features: ділитися траєкторіями

### Фаза 4
- [ ] AI-підказки на основі траєкторії
- [ ] Генерація персональних легенд
- [ ] Інтеграція з іншими модулями (Kazkar, PoDija)

---

## 🧪 Тестування

### Запуск backend
```bash
cd backend
python main.py
# Доступ: http://localhost:5000/api/ci/legend
```

### Запуск frontend
```bash
cd frontend
npm run dev
# Доступ: http://localhost:3000/ci/legend
```

### Build production
```bash
cd frontend
npm run build
# Вихід: dist/
```

---

## 📚 Контент легенди

### 20 вузлів (короткий огляд)

1. **Тиша і Можливість** — У тиші немає порожнечі
2. **Іскра** — Вибір уваги народжує подію
3. **Ритми** — Усе рухається хвилями
4. **Знаки** — Форма зберігає сенс
5. **Число** — Число — це міра взаємодії
6. **Геометрія** — Форма визначає шлях
7. **Поля** — Ніщо не існує ізольовано
8. **Дуальність** — Відображення створює рівновагу
9. **Людина** — Ми — жива симетрія
10. **Мережа** — Зв'язки важливіші за елементи
11. **Час** — Час — це спосіб узгодження змін
12. **Пам'ять** — Досвід має вагу
13. **Знання** — Спостереження переходить у творіння
14. **Зцілення** — Намір синхронізує системи
15. **Резонанс** — Відгук підсилює форму
16. **Системи** — Ціле більше за суму частин
17. **Усвідомлення** — Інтеграція знімає напругу
18. **Дані** — Сліди формують шлях
19. **Діалог** — Істина виникає між
20. **Простір** — Простір проживається

### Легенда про дуальність (10 розділів)

1. 🌑 Тиша і перша іскра
2. ☀️ Сонце і Місяць
3. ☁️ Небо і Земля
4. 🔥 Вогонь і Вода
5. ☯️ Інь і Ян
6. 🤲 Дві половини одного цілого
7. 🌓 Добро і Зло
8. 🌊⚛️ Хвиля і Частинка
9. 0️⃣1️⃣ 0 і 1
10. ♾ Великий Баланс

---

## 📖 Філософія реалізації

### Принципи

1. **Прогресивне розкриття сенсу** — користувач бачить спочатку короткий текст, потім може перейти до глибинного
2. **Нелінійна навігація** — можна рухатися будь-яким шляхом, а не лише послідовно
3. **Персоналізована траєкторія** — система запам'ятовує шлях кожного користувача
4. **Візуальна чіткість** — кожен елемент має чітке візуальне значення
5. **Емоційний резонанс** — дизайн викликає відчуття тепла, золота, світла

### Метафори

- **Центр (Ci)** — ядро системи, початок всього
- **Радіальна карта** — всесвіт знань, де все пов'язано
- **Вузли** — острови сенсу в океані можливостей
- **Зв'язки** — шляхи між ідеями
- **Шари тексту** — різні рівні глибини пізнання

---

## ✅ Статус завершення

- [x] Backend: контент та API
- [x] Frontend: компоненти та навігація
- [x] Інтеграція: роутинг та зв'язки
- [x] Анімації та стилізація
- [x] Responsive дизайн
- [x] Персоналізація та прогрес
- [x] Build успішний
- [ ] Документація користувача
- [ ] Розгортання production

---

**Створено**: 24 грудня 2024  
**Версія**: 1.0.0  
**Автор**: Cimeika Development Team  
**Ліцензія**: Internal

---

_Легенда ci — це не просто текст. Це живий простір, де кожен може знайти свій шлях до розуміння. ✨_
